[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "CryptoCoreUtils"
version = "0.1.0"
dependencies = [
    "pycryptodome==3.23.0",
    "numba==0.62.1",
    "numpy==2.3.5",
]